# Labs — Module 16: Image Registry

Thư mục này dùng để lưu toàn bộ code/config bạn tự viết khi làm theo [[../05-Labs|05-Labs.md]] của module này.

Gợi ý cấu trúc:

```
labs/
├── lab1-private-registry/
│   ├── docker-compose.yml
│   └── notes.md
├── lab2-harbor-install/
│   ├── harbor.yml
│   └── notes.md
└── lab3-image-scanning/
    └── scan-results/
```

Hãy lưu lại `notes.md` cho mỗi lab ghi chú lại: lỗi gặp phải, cách bạn tự sửa, và điều bạn hiểu thêm sau khi làm — đây là tài liệu ôn tập giá trị nhất khi bạn quay lại xem sau vài tháng.
